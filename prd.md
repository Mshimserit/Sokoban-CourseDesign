一、java推箱子游戏
技术要点：运算符+流程控制+数组+字符串+多线程+Swing
功能要求：
（1）使用Swing图形界面绘制游戏地图，包含墙块、箱子、玩家和目的地四种基本元素，地图数据从外部文件读取，支持多关卡切换。
（2）玩家通过键盘方向键控制人物上下左右移动，推动箱子至指定目的地，箱子只能被推不能被拉，一次只能推动一个箱子。
（3）游戏实时显示当前关卡编号、已走步数和已用时间，全部箱子归位后弹出通关提示并自动进入下一关。
（4）实现关卡编辑器：允许用户在图形界面上自行设计关卡地图，设计完成后保存到文件中供游戏加载使用。
（5）数据持久化：将每个关卡的最高分（最少步数）记录在文件中，支持排行榜展示。
要求将用户界面和应用逻辑分开两个包存放
src，存放所有源代码
classes，存放所有编译好的class文件
doc，存放项目相关文档
bin, 存放执行批处理文件
img, 存放图片目录
类和方法注释用文档注释/**……*/，对每个相对复杂的方法都需有相应的注释，最后自动生成文档(加分)；其他代码行需要的地方要加上适当的注释描述。
任务要求：
（1）根据实现的功能，划分出合理的对象类，明确对象类间的关系，绘制类图。
（2）为每个对象类设计正确的域和方法，为每个方法设计合理的方法体。同时，为对象类及内部的域和方法运用正确的修饰符，如访问控制符等。
（3）单机版程序要打包成Jar文件或者.exe进行发布